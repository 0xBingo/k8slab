coder:
  env:
    - name: CODER_PG_CONNECTION_URL
      valueFrom:
        secretKeyRef:
          name: coder-db-url
          key: url
    - name: CODER_ACCESS_URL
      value: "{{ coder_db_http_url }}"

nodeSelector:
    kubernetes.io/hostname: "{{ desired_node_name }}"
